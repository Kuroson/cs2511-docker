name: cs2511-gradle-9.2-ass1-cache CI

on:
  push:
    branches: [main]
    paths:
      - 'cs2511-gradle-9.2-ass1-cache/**'
      - '.github/workflows/cs2511-gradle-9.2-ass1-cache.yml'
  workflow_dispatch:

jobs:
  build:
    name: Build and publish cs2511-gradle-9.2-ass1-cache Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Log in to Docker Hub
        uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Extract short SHA
        id: vars
        run: echo "SHA_SHORT=${GITHUB_SHA::7}" >> $GITHUB_ENV
      - name: Build and push Docker image
        uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
        with:
          push: true
          # Self-contained: the cache folder holds both the Dockerfile and the
          # seed Gradle project it warms.
          context: ./cs2511-gradle-9.2-ass1-cache
          tags: |
            cs2511docker/cs2511-gradle-9.2-ass1-cache:latest
            cs2511docker/cs2511-gradle-9.2-ass1-cache:${{ env.SHA_SHORT }}
